# System Architecture - User Management CRUD

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         AZURE CLOUD                              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Azure Virtual Machine                   │  │
│  │                   (Ubuntu 22.04 LTS)                       │  │
│  │                                                             │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │              Apache2 Web Server (Port 80)           │  │  │
│  │  │                                                       │  │  │
│  │  │  Web Root: /var/www/html                            │  │  │
│  │  │  ┌────────────────────────────────────────────────┐ │  │  │
│  │  │  │  PHP Pages (Front-End)                         │ │  │  │
│  │  │  │                                                  │ │  │  │
│  │  │  │  • index.php           (Home/Dashboard)        │ │  │  │
│  │  │  │  • manage_users.php    (View All Users)        │ │  │  │
│  │  │  │  • add_user.php        (Create User)           │ │  │  │
│  │  │  │  • edit_user.php       (Update User)           │ │  │  │
│  │  │  │  • delete_user.php     (Delete User)           │ │  │  │
│  │  │  │  • db_test.php         (Test Connection)       │ │  │  │
│  │  │  │  • info.php            (PHP Info)              │ │  │  │
│  │  │  └────────────────────────────────────────────────┘ │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                            ↕                                │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │          MySQL Database Server (Port 3306)          │  │  │
│  │  │                                                       │  │  │
│  │  │  Database: webdb                                    │  │  │
│  │  │  User: webuser / Pass: webpass123                  │  │  │
│  │  │                                                       │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │  Table: users                               │   │  │  │
│  │  │  │                                              │   │  │  │
│  │  │  │  Columns:                                   │   │  │  │
│  │  │  │  • id (INT, PRIMARY KEY)                   │   │  │  │
│  │  │  │  • name (VARCHAR(100))                     │   │  │  │
│  │  │  │  • email (VARCHAR(100))                    │   │  │  │
│  │  │  │  • created_at (TIMESTAMP)                  │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              Network Security Group (Firewall)            │  │
│  │                                                             │  │
│  │  • Port 22  (SSH)    - Management Access                  │  │
│  │  • Port 80  (HTTP)   - Web Traffic                        │  │
│  │  • Port 443 (HTTPS)  - Secure Web (Optional)             │  │
│  │  • Port 3306 (MySQL) - Database (Restricted)             │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
                   ┌──────────────────────┐
                   │   Public Internet    │
                   │   (Users Access)     │
                   └──────────────────────┘
```

## 🔄 CRUD Operations Flow

### CREATE (Add User)
```
User Browser
    ↓ (1) GET request
add_user.php (Display Form)
    ↓ (2) POST request with name & email
add_user.php (Process Form)
    ↓ (3) SQL: INSERT INTO users
MySQL Database
    ↓ (4) Success/Error Response
add_user.php (Display Result)
    ↓ (5) Render HTML
User Browser (Shows success message)
```

### READ (View Users)
```
User Browser
    ↓ (1) GET request
manage_users.php
    ↓ (2) SQL: SELECT * FROM users
MySQL Database
    ↓ (3) Return all records
manage_users.php
    ↓ (4) Generate HTML table
User Browser (Display all users)
```

### UPDATE (Edit User)
```
User Browser
    ↓ (1) Click Edit button (GET with ?id=X)
edit_user.php
    ↓ (2) SQL: SELECT * FROM users WHERE id=X
MySQL Database
    ↓ (3) Return user data
edit_user.php (Display pre-filled form)
    ↓ (4) POST updated data
edit_user.php (Process update)
    ↓ (5) SQL: UPDATE users SET name=?, email=? WHERE id=X
MySQL Database
    ↓ (6) Success/Error Response
edit_user.php (Display result)
    ↓ (7) Render HTML
User Browser (Shows update confirmation)
```

### DELETE (Remove User)
```
User Browser
    ↓ (1) Click Delete button
JavaScript Confirmation Dialog
    ↓ (2) If confirmed: GET request with ?id=X
delete_user.php
    ↓ (3) SQL: SELECT name FROM users WHERE id=X
MySQL Database
    ↓ (4) Return user info (for confirmation)
delete_user.php
    ↓ (5) SQL: DELETE FROM users WHERE id=X
MySQL Database
    ↓ (6) Success/Error Response
delete_user.php (Display result)
    ↓ (7) Render HTML
User Browser (Shows deletion confirmation)
```

## 🛠️ Technology Stack

```
┌─────────────────────────────────────────┐
│         Technology Layers               │
├─────────────────────────────────────────┤
│  Frontend:   HTML5, CSS3, JavaScript   │
│  Backend:    PHP 8.x                    │
│  Database:   MySQL 8.x                  │
│  Web Server: Apache2                    │
│  OS:         Ubuntu 22.04 LTS           │
│  Cloud:      Microsoft Azure            │
│  IaC:        Terraform                  │
│  Init:       Cloud-init                 │
└─────────────────────────────────────────┘
```

## 📊 Data Flow Diagram

```
                    ┌──────────────┐
                    │   Browser    │
                    └──────┬───────┘
                           │
                    HTTP Request (GET/POST)
                           │
                           ↓
                    ┌──────────────┐
                    │  Apache2     │
                    │  Port 80     │
                    └──────┬───────┘
                           │
                    Forward to PHP
                           │
                           ↓
                    ┌──────────────┐
                    │  PHP Engine  │
                    │  (mod_php)   │
                    └──────┬───────┘
                           │
                    Process & Connect
                           │
                           ↓
                    ┌──────────────┐
                    │    MySQL     │
                    │  (webdb)     │
                    └──────┬───────┘
                           │
                    Execute Query
                           │
                           ↓
                    ┌──────────────┐
                    │  users table │
                    │  (CRUD ops)  │
                    └──────┬───────┘
                           │
                    Return Data
                           │
                           ↓
                    ┌──────────────┐
                    │  PHP Format  │
                    │  HTML Output │
                    └──────┬───────┘
                           │
                    HTTP Response
                           │
                           ↓
                    ┌──────────────┐
                    │  Browser     │
                    │  Renders UI  │
                    └──────────────┘
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: Azure Network Security Group                  │
│           • Firewall rules                               │
│           • IP filtering                                 │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 2: Apache Configuration                          │
│           • Virtual hosts                                │
│           • Directory permissions                        │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 3: PHP Security                                   │
│           • Input validation (TODO)                      │
│           • Prepared statements (TODO)                   │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Layer 4: MySQL Security                                 │
│           • User permissions                             │
│           • Database isolation                           │
└─────────────────────────────────────────────────────────┘
```

## 📁 File Structure

```
/var/www/html/
├── index.php              [Dashboard]
├── manage_users.php       [List Users - READ]
├── add_user.php           [Add User - CREATE]
├── edit_user.php          [Edit User - UPDATE]
├── delete_user.php        [Delete User - DELETE]
├── db_test.php            [DB Connection Test]
└── info.php               [PHP Configuration]

MySQL Database (webdb)
└── users table
    ├── id (Primary Key)
    ├── name
    ├── email
    └── created_at
```

## 🚦 Request/Response Cycle

```
1. User Action
   └→ Click button/link in browser

2. HTTP Request
   └→ GET or POST to PHP page

3. Apache Receives Request
   └→ Routes to appropriate PHP file

4. PHP Processes Request
   └→ Parse form data
   └→ Connect to MySQL
   └→ Execute SQL query

5. MySQL Executes Query
   └→ SELECT, INSERT, UPDATE, or DELETE
   └→ Return result set or status

6. PHP Formats Response
   └→ Generate HTML
   └→ Apply CSS styling
   └→ Add JavaScript (if needed)

7. Send HTTP Response
   └→ Status code (200, 404, 500, etc.)
   └→ HTML content

8. Browser Renders
   └→ Display formatted page
   └→ Execute client-side JS
   └→ Apply CSS styles
```

## 🎨 Component Breakdown

```
┌──────────────────────────────────────────────────┐
│              User Interface                       │
├──────────────────────────────────────────────────┤
│  Navigation                                       │
│  ├── Home Button                                 │
│  ├── Manage Users Link                           │
│  └── Add User Link                               │
├──────────────────────────────────────────────────┤
│  Content Area                                     │
│  ├── Statistics Dashboard (manage_users.php)    │
│  ├── User Table (manage_users.php)              │
│  ├── Add Form (add_user.php)                    │
│  ├── Edit Form (edit_user.php)                  │
│  └── Delete Confirmation (delete_user.php)      │
├──────────────────────────────────────────────────┤
│  Action Buttons                                   │
│  ├── Add (Create)                                │
│  ├── Edit (Update)                               │
│  ├── Delete (Remove)                             │
│  └── Back/Cancel                                 │
└──────────────────────────────────────────────────┘
```

## 🔄 Session Flow

```
Session Start: User visits index.php
    │
    ├─→ View PHP Info
    │   └─→ info.php (Display server info)
    │
    ├─→ Test Database
    │   └─→ db_test.php (Verify connection)
    │
    ├─→ Manage Users
    │   └─→ manage_users.php
    │       ├─→ Add New User → add_user.php
    │       ├─→ Edit User → edit_user.php?id=X
    │       └─→ Delete User → delete_user.php?id=X
    │
    └─→ Add User Directly
        └─→ add_user.php

All paths can return to index.php
```

---

**Architecture Version:** 1.0  
**Last Updated:** 2025-11-02  
**Status:** Production Ready (with security enhancements needed)
